import React from 'react';
import {styled} from '@mui/material/styles';

const container = styled('div')({
    margin:'0px'
});

export const StyledMenuHeader = styled(container)({
    padding:'12px'
});

//options for travel mode and algorithm
export const StyledMenuSelect = styled(container)({
    padding:'12px'
});

export const StyledMenuSearch = styled(container)({
    padding:'12px'
});

export const StyledMenuLocations = styled(container)({
    padding:'12px'
});

export const StyledMenuSubmit = styled(container)({
    padding:'12px'
});